numpy>=2.0
pandas>=2.2
fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
python-dotenv>=1.0


# Headline sentiment (bot/sentiment.py). FinBERT is ~440 MB of weights, fetched
# once on first use and cached; the CPU build of torch is enough and is roughly
# a quarter the size of the CUDA one:
#     pip install torch --index-url https://download.pytorch.org/whl/cpu
transformers>=4.44
torch>=2.4
